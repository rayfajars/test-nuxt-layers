{
  "name": "test-nuxt-layers",
  "version": "0.1.0",
  "main": "./nuxt.config.ts",
  "files": [
    "nuxt.config.ts",
    "components",
    "composables",
    "utils",
    "plugins",
    "assets"
  ],
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "prepare": "nuxt prepare"
  },

  "dependencies": {
    "date-fns": "^4.1.0",
    "@nuxtjs/tailwindcss": "^6.14.0"
  },

  "peerDependencies": {
    "nuxt": "^4.0.0"
  },

  "devDependencies": {
    "nuxt": "^4.3.1"
  }
}