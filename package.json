{
  "name": "test-nuxt-layers",
  "version": "0.1.2",
  "main": "./nuxt.config.ts",
  "files": [
    "nuxt.config.ts",
    "app",
    "tailwind.config.ts"
  ],
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "prepare": "nuxt prepare"
  },

  "dependencies": {
    "date-fns": "^4.1.0"
  },

  "peerDependencies": {
    "nuxt": "^4.0.0",
    "@nuxtjs/tailwindcss": "^6.14.0"
  },

  "devDependencies": {
    "nuxt": "^4.4.2",
    "@nuxtjs/tailwindcss": "^6.14.0"
  }
}